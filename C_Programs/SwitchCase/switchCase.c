#include <stdio.h>
int main()
{
    float val;
    printf('Enter value for Ritcher scale: ');
    scanf('%',&val);

    if(val < 4.5)
        

 


    switch (val)
    {
    case 0 ... 4:
        printf("%d in range 1 to 6\n", val);
        break;
    case 4 ... 6:
        printf("%d in range 19 to 20\n", val);
        break;
    default:
        printf("%d not in range\n", val);
        break;
    }

    return 0;
}